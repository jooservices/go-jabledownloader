#!/usr/bin/env bash
# Run docker compose as the host runner user (avoids root-owned artifacts on self-hosted runners).
set -euo pipefail

export DOCKER_UID="${DOCKER_UID:-$(id -u)}"
export DOCKER_GID="${DOCKER_GID:-$(id -g)}"

exec docker compose "$@"